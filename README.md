# Will React Native App Template

## Installation

```bash
uv venv && . .venv/bin/activate
cd src/backend
poetry install
cd ../mobile
npm install
cd ../..
```

## Run the App

Use default build task: `Cmd-Shift-B` on Mac
